@* =====================================================================================
   Name: Luc de Marillac St Julien
   Student Number: ST10382638
   Group: 1

   References:
     1) Project outline/instructions: https://chatgpt.com/c/691e5672-00c8-8328-b4da-a2c0b7ea1c63
     2) C# Reference & Tutorials:   https://www.w3schools.com/cs/index.php
   ===================================================================================== *@

@using ST10382638_PROG_POE.Models
@model ApplicationUser

@{
    ViewData["Title"] = "Edit User";

    // Data provided from HRController.Edit:
    // - ViewBag.Role: current primary role (e.g. Lecturer, Coordinator, Manager, HR)
    // - ViewBag.LecturerProfile: lecturer-specific data including HourlyRate
    var role = ViewBag.Role as string ?? "";
    var lecturerProfile = ViewBag.LecturerProfile as LecturerProfile;
}

<div class="page-head">
    <h1>Edit User</h1>
    <p class="page-sub">Update user details, password and lecturer hourly rate</p>
</div>

<div class="glass-card p-3">
    @* Form posts back to HR/Edit (POST) with anti-forgery protection *@
    <form asp-action="Edit" method="post" class="hover-only-inputs">
        @Html.AntiForgeryToken()

        @* Hidden Id required for binding the correct ApplicationUser *@
        <input asp-for="Id" type="hidden" />

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="row g-3">
            @* ---------- User details (all roles) ---------- *@
            <div class="col-12">
                <h5 class="mb-2">User Details</h5>
                <hr class="hr-neon" />
            </div>

            <div class="col-md-6">
                <label asp-for="FirstName" class="form-label"></label>
                <input asp-for="FirstName" class="input-clean" />
                <span asp-validation-for="FirstName" class="text-danger"></span>
            </div>

            <div class="col-md-6">
                <label asp-for="Surname" class="form-label"></label>
                <input asp-for="Surname" class="input-clean" />
                <span asp-validation-for="Surname" class="text-danger"></span>
            </div>

            <div class="col-md-6">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" class="input-clean" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            @* ---------- Password (all roles) ---------- *@
            <div class="col-12 mt-2">
                <h5 class="mb-2">Password</h5>
                <hr class="hr-neon" />
            </div>

            <div class="col-md-6">
                <label class="form-label">New Password</label>
                <input type="password" name="NewPassword" class="input-clean" />
            </div>

            <div class="col-md-6">
                <label class="form-label">Confirm Password</label>
                <input type="password" name="ConfirmPassword" class="input-clean" />
                @Html.ValidationMessage("ConfirmPassword", null, new { @class = "text-danger" })
            </div>

            @* ---------- Lecturer-only hourly rate ---------- *@
            @if (role == "Lecturer")
            {
                <div class="col-12 mt-2">
                    <h5 class="mb-2">Lecturer Settings</h5>
                    <hr class="hr-neon" />
                </div>

                <div class="col-md-6">
                    <label class="form-label">Hourly Rate</label>
                    <input name="HourlyRate"
                           type="number"
                           step="0.01"
                           class="input-clean"
                           value="@(lecturerProfile != null ? lecturerProfile.HourlyRate.ToString("0.00") : string.Empty)" />
                </div>
            }
        </div>

        @* ---------- Actions ---------- *@
        <div class="d-flex gap-12 mt-16">
            <button type="submit" class="btn btn-primary">Save Changes</button>
            <a asp-action="Index" class="btn btn-outline">Back</a>
        </div>
    </form>
</div>

@section Scripts {
    @* Client-side validation scripts for this form *@
    <partial name="_ValidationScriptsPartial" />
}

@* ---------------------------------...ooo000 END OF FILE 000ooo...--------------------------------- *@
